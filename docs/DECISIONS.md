# Decisions and verifications

Append-only, newest last. Each entry: what was decided or verified, and the evidence. Add a row whenever you settle a price, a model ID, a licence, an endpoint or a design choice.

## 2026-09-22 — kit setup, before any code

### Decisions
| # | Decision | Reason |
|---|---|---|
| D1 | Local-first architecture; AWS limited to S3, Bedrock, IAM and Budgets, with Lambda as an optional stretch | 3 days and about $50; the blueprint's managed services bill for idle capacity |
| D2 | FAISS (faiss-cpu) plus SQLite FTS5, no managed vector database | exact search is fast at MVP scale and costs nothing |
| D3 | `BAAI/bge-m3` embeddings and `BAAI/bge-reranker-v2-m3` reranking | multilingual and long-context; replaces `all-MiniLM-L6-v2`, which is English-only and truncates at 256 word pieces |
| D4 | No LangChain or LlamaIndex | the reports' splitter sample doesn't run as written, and plain Python is simpler to test |
| D5 | Judgments come from the AWS Open Data bucket rather than scraping or a paid API | free, licensed, already structured |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V1 | Indian Supreme Court judgments: `s3://indian-supreme-court-judgments`, region ap-south-1, CC-BY-4.0, readable with `--no-sign-request`, 1950–2025, per-year metadata parquet, English and regional PDFs | https://registry.opendata.aws/indian-supreme-court-judgments/ · https://github.com/vanga/indian-supreme-court-judgments |
| V2 | Indian High Court judgments: `s3://indian-high-court-judgments`, 25 High Courts, roughly 17.8M judgments and 1.25 TiB of tar archives | https://registry.opendata.aws/indian-high-court-judgments/ · https://github.com/vanga/indian-high-court-judgments |
| V3 | Indore Development Authority v. Manoharlal is a Constitution Bench decision of 6 March 2020, (2020) 8 SCC 129 — not 2023 | https://api.sci.gov.in/supremecourt/2022/7205/7205_2022_4_1505_42249_Judgement_24-Feb-2023.pdf · https://lawlex.org/?p=25138 |
| V4 | The Aalap instruction dataset is gated, English-only, partly generated with GPT-4 and gpt-3.5-turbo, and licensed per source subset | https://huggingface.co/datasets/opennyaiorg/aalap_instruction_dataset |
| V5 | Indian Kanoon API is prepaid: about ₹0.50 per search, ₹0.20 per document, ₹500 free on signup, ₹10,000 per month for verified non-commercial use | https://indiankanoon.org/members/pricing |
| V6 | Amazon Textract's documented languages are English, Spanish, Italian, Portuguese, French and German — no Indic scripts. Re-check the quotas page, the answer is dated | https://repost.aws/questions/QUVeFLYhy8RFWKRH0H94fXVA |
| V7 | Whether credits cover Bedrock third-party models depends on the credit programme, and some usage is billed through AWS Marketplace | https://repost.aws/questions/QULMmXNMRwRvGSYm6OGt6e3Q/aws-credits-for-bedrock · https://aws.amazon.com/aws-startups/learn/aws-activate-credits-now-accepted-for-third-party-models-on-amazon-bedrock/ |
| V8 | Copyright Act 1957 s. 52(1)(q): (ii) an Act may be reproduced when published with commentary or other original matter; (iv) judgments and orders may be reproduced unless the court prohibits it | https://future.indiankanoon.org/doc/648494 |
| V9 | Claude Code loads the project `CLAUDE.md` at the start of every session, recommends under about 200 lines, and expands `@path` imports at launch | https://code.claude.com/docs/en/memory |
| V10 | `opennyaiorg/InJudgements_dataset` exists (about 12k rows); its licence was not confirmed from the card | https://huggingface.co/opennyaiorg |

### Still to verify during the build
- Current India Code URLs and download behaviour for each seed Act.
- Licences on the InJudgements, InRhetoricalRoles and InLegalNER dataset cards.
- Bedrock models and inference profiles available in ap-south-1, and current token prices.
- AWS Budgets pricing, and API Gateway timeouts if API Gateway is ever considered.
- Tesseract traineddata available on this machine for the demo languages.
- Commencement notifications for the BNS, BNSS, BSA and the Consumer Protection Act 2019, including excluded provisions.

## 2026-09-23 — Phase 0 setup

### Decisions
| # | Decision | Reason |
|---|---|---|
| D6 | Project lives at `C:\dev\praetor-ai` in its own git repo (`main`), not in `OneDrive\Desktop` | OneDrive would sync the venv, corpora, SQLite and FAISS files (locking risk for SQLite), and the old folder sat inside an accidental git repo rooted at the home directory |
| D7 | Python 3.12 (uv-managed, user-level, no admin) with `uv.lock`; torch from the PyTorch `cu130` index via `[tool.uv.sources]` — resolved to `torch 2.14.0+cu130` | only Python 3.14 was installed system-wide; 3.12 has the broadest wheel coverage. cu130 includes `sm_120` and was already proven on this GPU (global `torch 2.13.0+cu130`) |
| D8 | **The GPU has 8 GB, not 12 GB** (RTX 5070 *Laptop*, 7.93 GiB usable). bge-m3 in fp16 peaks at 1.09 GiB when converted to fp16 on the CPU before moving to CUDA (2.62 GiB if moved in fp32 first). Local LLM candidates must fit in ~5 GB: `qwen3.5:4b` (3.4 GB) and `gemma4:e2b-it-qat` (4.3 GB) were pulled. `gemma4:latest` (9.6 GB, already on the machine) can only run partly offloaded to CPU. Phase 1 uses `qwen3.5:4b` provisionally; the pick is made by the Phase 2 benchmark | measured with `praetor gpu-check` and `nvidia-smi` |
| D9 | Tesseract deferred: not installed, and the installer needs admin. Pages that need OCR are rejected with a logged reason, never indexed as garbage | the Phase 1 corpus is English PDFs with text layers |
| D10 | CLI built on stdlib `argparse` | no new dependency needed |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V11 | India Code moved from `www.indiacode.nic.in` to `https://indiacode.gov.in`; the old domain now serves only a migration notice. The new site runs DSpace 9.1, with a public REST API at `/server/api` (search: `/discover/search/objects`, items: `/core/items/{uuid}`, files: `/core/bitstreams/{uuid}/content`) | GET https://www.indiacode.nic.in/ and https://indiacode.gov.in/server/api, 2026-09-23 |
| V12 | Central Act items (`dc.identifier.state_name = CENTRAL`): Registration Act 1908 handle `123456789/496068`; Transfer of Property Act 1882 `123456789/496421`; Consumer Protection Act 2019 `123456789/496115`. The ORIGINAL bundle holds the English `a<year>-<no>.pdf` and Hindi `H<year>-<no>.pdf` with MD5 checksums. State copies are separate items whose `dc.identifier.refact` points to the central `act_id`. Per-section items (collection `SECTION`) carry `section_number` and the section text | API responses, 2026-09-23; IDs recorded in `data/registry/sources.yaml` |
| V13 | SC judgments bucket layout: `metadata/parquet/year=YYYY/metadata.parquet` (~1 MB for 2023); `data/pdf/year=YYYY/english/<name>_EN.pdf` (854 files for 2023) and `regional/<name>_<LANG>.pdf` (e.g. `HIN`, `PUN`); `data/tar/year=YYYY/{english,regional}/*.tar` with `*.index.json` | `aws s3 ls --no-sign-request --region ap-south-1`, 2026-09-23 |
| V14 | Ollama library sizes: `qwen3.5:4b` 3.4 GB, `qwen3.5:9b` 6.6 GB, `gemma4:e2b-it-qat` 4.3 GB, `gemma4:e4b-it-qat` 6.1 GB, `gemma4:12b-it-qat` 7.2 GB | https://ollama.com/library/qwen3.5/tags · https://ollama.com/library/gemma4/tags, 2026-09-23 |
| V15 | The migrated India Code site's terms of use could not be located (`/info/end-user-agreement` carries none). Licence field says so; still CHECK | https://indiacode.gov.in/info/end-user-agreement, 2026-09-23 |

## 2026-09-23 — Phase 1 build

### Decisions
| # | Decision | Reason |
|---|---|---|
| D11 | Until the Phase 2 reranker gate exists, the evidence gate runs on the top dense cosine: `MIN_DENSE_SCORE=0.60`, provisional | Known-item questions scored 0.70–0.71 (correct section at rank 1); out-of-corpus questions (passport, IPC murder, capital-gains tax) topped out at 0.50–0.55. n=7 — calibrate on the eval dev split in Phase 2 |
| D12 | SQLite access lives in a new `app/store/` module (documents, chunks + FTS5, ingest_runs, rejects, cache, spend); chunk columns are generated from the `Chunk` dataclass | one owner for the schema, used by both ingestion and retrieval |
| D13 | SCR headnotes, case-law lists and counsel are not indexed; judgments are indexed from the "delivered by" / "Judgment / Order of the Supreme Court" marker onwards. Headnotes in the metadata `raw_html` are used only to select judgments. The case-header chunk is rendered from the dataset record with `text_source = metadata` (a third allowed value besides `layer` and `ocr`) | headnotes are editorial additions (data-sources licensing notes) and not the court's reasoning |
| D14 | India Code `STATE AMENDMENT(S)` blocks become separate chunks with the state's ISO 3166-2 code as `jurisdiction` (`data/registry/jurisdictions.yaml`); an unmapped state name is rejected, never guessed. Central section text never contains them | state amendments are state law; mixing them into central text would misstate the law |
| D15 | Judgment locators are paragraph ranges (`paras 12-15`, `para 7 (part 2 of 3)`) when numbering is detected, and page ranges (`pp. 13-16`) when one "paragraph" would span more than 3 chunks (numbering lost mid-judgment) | a label like "para 11 (part 47 of 73)" would mislead a reader |
| D16 | `doc_id` canonical keys: Acts = India Code handle URL + `#<lang>`; judgments = the `s3://` key. Judgment `source_url` = the public HTTPS object URL; Act `source_url` = the handle page | stable across re-downloads; clickable in citations |
| D17 | Provisional local answer model for Phase 1: `qwen3.5:4b` with `think: false`, temperature 0.1, `num_ctx` 8192 — runs 100% on GPU (3.3 GB), warm answers 7–14 s | fits the 8 GB budget next to bge-m3; the Phase 2 benchmark decides |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V16 | India Code stamps a rotated "India Code" watermark (Helvetica-Bold 27–37 pt, grey, 45°) into each PDF as it is served, so the bytes differ from the repository copy: `a1908-16.pdf` is 870,914 B served vs 702,521 B / MD5 `85c5ae58…` in the repository. Without filtering, watermark glyphs land inside words (`appointmIents`) | download + char inspection with pdfplumber, 2026-09-23 |
| V17 | Consumer Protection Act 2019 commencement, from the footnote to s. 1(3) in the India Code text: most provisions 20 July 2020 (S.O. 2351(E), 15 July 2020); listed provisions 24 July 2020 (S.O. 2421(E), 23 July 2020) | `A2019-35.pdf` p. 6; recorded in `statutes.yaml` |
| V18 | ISO 3166-2:IN codes after the 2023-11-23 change: IN-OD, IN-CG, IN-TS, IN-UK (formerly IN-OR, IN-CT, IN-TG, IN-UT) | https://en.wikipedia.org/wiki/ISO_3166-2:IN |
| V19 | SC metadata parquet: 18 columns (`title, petitioner, respondent, description, judge, author_judge, citation, case_id, cnr, decision_date, disposal_nature, court, available_languages, raw_html, path, nc_display, scraped_at, year`); `decision_date` is DD-MM-YYYY; `author_judge` always null; `raw_html` holds the coram (author marked `*`) and headnotes; PDF key = `data/pdf/year=<year>/english/<path>_EN.pdf`. 2016–2025: 7,997 rows, no duplicate `cnr` or `path` | `docs/reports/sc_metadata_profile.md` |
| V20 | Two SCR PDF layouts: older (to ~2023) with A–H margin letters and "The Judgment of the Court was delivered by"; Digital SCR (2024+) with "Judgment / Order of the Supreme Court", hanging paragraph numbers, and a "Result of the case" / "Headnotes prepared by" tail. Some older PDFs carry an OCR text layer with recognition errors | inspection of the 50 downloaded PDFs |
| V21 | Bucket objects are readable over plain HTTPS (`https://indian-supreme-court-judgments.s3.ap-south-1.amazonaws.com/data/pdf/...` → 200 application/pdf); India Code handle pages resolve with GET (HEAD returns 405) | requests on 2026-09-23 |

## 2026-09-23/24 — Phase 2 build

### Decisions
| # | Decision | Reason |
|---|---|---|
| D18 | Corpus: 12 central Acts (the 3 Phase 1 Acts + RFCTLARR, RERA, Limitation, Contract, Specific Relief, CPC, BNSS, and BNS/BSA beyond the seed list for transition questions) and 1,000 SC judgments 2016–2025 selected by headnote Act mentions, round-robin across Acts, newest first, plus the landmark IDA v. Manoharlal. Judgments came from the English **year tars** (user's choice, 2026-09-23): 10 tars, 3.3 GB, one at a time into `data/scratch/`, only selected members extracted, each tar deleted after | user asked for ~1,000; the dataset README asks for tars for bulk downloads (V25) |
| D19 | "In re" judgments have no respondent; the dataset renders them `<petitioner> versus .`. The case title drops the empty side instead of storing a placeholder party (the validator had rightly rejected all 189 chunks of that judgment) | render the record faithfully without inventing |
| D20 | The CrPC 1973 text is **not ingested** (V23). The registry keeps it as repealed (successor BNSS), exact lookups of CrPC sections report that the text is absent, and questions naming it get the BNSS repeal section and closest BNSS provision pinned | an as-enacted 1974 scan would misstate the law as it stood |
| D21 | Judgments decided under repealed Acts (CrPC, CPA 1986, LA Act 1894) are selected on purpose | transition questions need them; statutes.yaml supplies their status |
| D22 | FTS5 tokenizer `unicode61 remove_diacritics 0 categories 'L* N* Co M*'`; existing indexes are migrated on connect | V22 |
| D23 | CPC First Schedule split into Order/rule chunks (`O. XXXIX r. 1`, chapter "Order XXXIX — ..."), Appendices as their own blocks; a trailing "Statement of Objects and Reasons" (RERA) is dropped; the table of contents is read only from "ARRANGEMENT OF SECTIONS" to the first schedule/Order heading | Order/rule citations are how CPC is cited; the CPC prints a numbered amending-Acts list before its arrangement |
| D24 | BNS stays `in_force` with `not_in_force: ["106(2)"]`; chunks of s. 106 get `partially_in_force` | the notification excepts one sub-section, not the Act (V24) |
| D25 | An alias names one Act (first id in aliases.yaml); exact section lookup never crosses to a successor; search expands to successors | section numbers differ between old and new codes |
| D26 | Not built in Phase 2 (cut list #2): LLM classification and LLM query rewriting. The soft domain filter is not applied (the reranker and statute quota cover it); Hindi queries rely on cross-lingual dense retrieval | time; measured Hindi Recall@5 is reported per language |
| D27 | Evidence gate on the top **rerank** score, `MIN_EVIDENCE_SCORE = 0.10`; exact/case lookups bypass it. Out-of-corpus questions scored 0.004–0.065; the lowest answerable 0.025 (eviction), 0.159, 0.198. **Chosen after seeing all 59 draft questions' scores**, so the test split is not an unbiased check of the gate; re-calibrate when the gold set is verified and larger | the dev-only optimum (0.027–0.039) rested on 2 points and would admit test out-of-corpus questions |
| D28 | Retrieval pool: dense top 50 + keyword top 50, plus a **statute quota** (top 10 statute chunks from each of dense and keyword as their own RRF lists), **Act-scoped** statute search when the query names an Act, **case-name lookup** (`X v. Y` against stored titles) and a **transition pin** (successor's repeal section + closest provision when a repealed Act is named) | judgments outnumber statute chunks ~8:1; see the ablation in `evaluation/reports/` |
| D29 | Final order = RRF over (fused rank, rerank rank), pinned hits first. On the same candidate pools, dev-split Recall@5: rerank-only 0.89, fused-only 0.93, RRF(fused, rerank) 0.93 (ties with score blends; chosen as parameter-free). Test split: 0.78 / 0.82 / 0.89 | the cross-encoder alone ranks judgment paraphrases above the statute text |
| D30 | Parsing runs in worker processes (`index --workers`, default 4) and releases each pdfplumber page after use | a 296-page judgment held 7.3 GB; with `page.close()` it parses within normal memory |
| D31 | Answer prompt `answer-v2`: a repeal stated in the sources goes into the short answer with the new law named; the validator keeps a bold heading when it removes the sentence under it | the 4B model otherwise answered "still available under s. 438 CrPC" from pre-2024 judgments |
| D32 | **Local answer model: `gemma4:latest`** (Ollama id `c6eb396dbd59`, 8.0B, Q4_K_M, Apache-2.0; loads as 3.2 GB, 100% GPU next to bge-m3 and the reranker). Benchmark on the 30 dev questions (26 answered), same pipeline: qwen3.5:4b — 3 extractive fallbacks, 3 authority removals, must-mention 90%, expected source cited 92%, p50 10.2 s; gemma4:e2b-it-qat — 1 / 6, 95%, 88%, 6.6 s; gemma4:latest — 0 / 0, 95%, 92%, 10.6 s. All three: 0 invalid citations after validation. With n=26 the differences are small; gemma4:latest was chosen for never needing the fallback and never naming an unsupported authority. `OLLAMA_MODEL` pins the tag; record the id if it is re-pulled | `evaluation/reports/20260923T233233Z` (qwen), `20260923T234816Z` (e2b, clean re-run), `20260923T234336Z` (latest) |
| D33 | **Correction to D8:** `gemma4:latest` is a 9.6 GB download but `ollama ps` shows it resident as 3.2 GB, 100% GPU; D8's statement that it "can only run partly offloaded to CPU" was an assumption from the download size and was wrong | measured 2026-09-24 |
| D34 | The first gemma4:e2b benchmark run is discarded: a side probe loaded a second model during it, Ollama's runner crashed, and one question fell back without a model attempt. Re-run on a clean GPU | eval row `reg-023-time-limit` had no Ollama attempt in `20260923T233744Z` |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V22 | SQLite FTS5 `unicode61` (default categories) splits Devanagari and Tamil words at combining marks (रजिस्ट्रीकरण → र, रज, स, ट, करण); adding `categories 'L* N* Co M*'` keeps whole words | fts5vocab test, `tests/unit/test_retrieval_phase2.py` |
| V23 | India Code has no current CrPC text: only repeal-register item `123456789/620185` "The Code of Criminal Procedure 1973, 02 of 1974 (Rep., Act 46 of 2023)" with `1974-2.pdf` (8.96 MB scan) whose extracted text is the as-enacted 1974 print with OCR errors and no later amendments | API + bitstream text, 2026-09-23 |
| V24 | Commencement from the Acts' own footnotes: BNS 1 July 2024 except s. 106(2) (S.O. 850(E), 23 Feb 2024); BNSS 1 July 2024 except the First Schedule entry for BNS s. 106(2) (S.O. 848(E)); BSA 1 July 2024 (S.O. 849(E)); RERA 1 May 2016 (S.O. 1544(E)) and 1 May 2017 (S.O. 1216(E)); repeal sections BNSS s. 531, BNS s. 358, BSA s. 170 | ingested texts; `statutes.yaml` |
| V25 | Dataset README: "Please prefer `data/tar/`, `metadata/tar/`, and `metadata/parquet/` for bulk downloads. Syncing `data/pdf/` … is intended for targeted access." `*.index.json` lists member file names per tar part, no byte offsets. English tars 2016–2025: 169–479 MB each | https://github.com/vanga/indian-supreme-court-judgments; `aws s3 ls`, 2026-09-23 |
| V26 | `BAAI/bge-reranker-v2-m3`: `model.safetensors` 2,166 MB, Apache-2.0; sentence-transformers `CrossEncoder` applies `Sigmoid()` (scores 0–1); fp16 on CUDA peaks at 1.09 GiB | HF API + load, 2026-09-23 |
| V27 | India Code repeal register: IPC "(Rep., Act 45 of 2023)" `123456789/488475`; IEA "(Rep., Act 47 of 2023)" `123456789/488783`; CPA 1986 "(Rep., Act 35 of 2019)" `123456789/491618`; LA Act 1894 "(Rep., Act 30 of 2013)" `123456789/489153` | API search, 2026-09-23 |
| V28 | Indore Development Authority v. Manoharlal is in the dataset: 2020 INSC 294, [2020] 3 S.C.R. 1, decided 06-03-2020 (matches V3); its answer 3 in para 363 reads the "or" in s. 24(2) as "nor"/"and" | metadata parquet 2020 + ingested text pp. 326-327 |

## 2026-09-24 — Audit and repairs after Phase 2 (local only; no AWS, no spend)

### Decisions
| # | Decision | Reason |
|---|---|---|
| D35 | **Root causes of the four reported failures, by the first stage where the authority is lost** (stage trace over the real index): (1) *anticipatory bail, English* — BNSS s. 482 is the top statute hit on dense and keyword and fused rank 1, but the reranker scores it 0.025 for "How do I apply for anticipatory bail?" (the provision never says "anticipatory"), final rank 12, outside the 8 context places; (2) *Hindi* — lost at first-stage retrieval (dense rank 715, no keyword hit; no Hindi text is indexed); (3) *s. 438 CrPC* — BNSS s. 531 is pinned, but the transition pin's "closest BNSS provision" (dense top-1) is not s. 482, and the reranker gives s. 482 0.002; (4) *RERA refund* — s. 18 is the top statute hit (fused 7) but the reranker (0.37) drops it to final 12, outside the context; (5) *eviction* — TPA s. 106 is final rank 1 and S1, but the top rerank score is 0.025 < 0.10, so the evidence gate refuses; (6) *lease notice* — TPA s. 106 is S1 at every stage; the variation is in generation (temperature 0.1, no seed, model defaults top_k 64 / top_p 0.95) plus the quote check removing a sentence quoted with an ellipsis. Separately, the "Jurisdiction and date" line: prompt `answer-v2` asked for `"based on texts retrieved on <date>"` in quotation marks, the model copied them, the quote check removed the line in 45 of 53 answers, and `changed` forced confidence to low (31 of 52 model answers had no other change) | `evaluation/reports/diagnostics/20260924_stage_trace_before.json`; eval `20260924T002232Z` |
| D36 | **Retrieval repairs.** (a) `data/registry/legal_terms.yaml`: four lay-term → statutory-wording entries (anticipatory bail → "bail to person apprehending arrest"; home buyer → allottee / promoter; eviction → determination of lease / notice to quit; landlord / tenant → lessor / lessee), each with evidence from the Acts or ingested judgments. Search expansion only — keyword phrases, a second dense query with its own statute quota, the Act-scoped and transition-pin ranking, and the reranker query — in fused modes only; it never adds a section or an Act. Hindi terms are marked unverified. (b) Context statute slots: the two statute chunks with the best fused rank (≤ 10) always get context places, displacing the lowest non-statute passages. (c) FTS5 Act-title phrases keep stop-words: `"transfer property act 1882"` matched 0 chunks against 260 for the printed title, so the Act-title boost never worked for the TPA, CrPC, RERA, CPC or RFCTLARR. The term entries were chosen after seeing the failures, so gold-set gains that depend on them are a regression check, not an unbiased estimate | D35; unit tests `test_context_slots.py`, `test_pipeline_rules.py`, `test_retrieval_phase2.py` |
| D37 | **Decoding and caching.** Answers use temperature 0 with seed 42 (`LLM_TEMPERATURE`, `LLM_SEED`), recorded in every trace; eval reports record the git commit, gold and registry hashes, index corpus hash, prompt version, model digest and retrieval settings. There is no answer cache: nothing reads or writes the `cache` table and `CACHE_ENABLED` has no effect (kept for the Phase 4 API), so "cache on versus bypassed" does not arise. `scripts/repeat_answers.py` runs a question N times and hashes context, raw output and final answer per run | code search and `ollama show gemma4:latest`, 2026-09-24 |
| D38 | **Gold set.** Every row has a `group` (paraphrases of one question), and `load_gold` refuses a group split across dev and test. Moved to keep groups whole: `hi-bnss-482-anticipatory` and `tr-438-anticipatory` to dev (the group was traced to design D36); `hi-ica-073-damages` and `cl-unregistered-agreement` to test (30 dev / 29 test). All Phase 2 percentages are **exploratory**: the 59 questions are unverified drafts, and every question and the 0.10 threshold (D27) have been seen. `evaluation/verification_sheet.csv` is for a person to verify; no question is marked verified | `scripts/make_verification_sheet.py` |
| D39 | **Validator and prompt `answer-v3`.** The system renders the jurisdiction-and-date line from the cited chunks' metadata and strips any model-written one before validation. A section named with an Act ("s. 482 CrPC") must be attested for that Act — a cited statute chunk of that Act and section, or a cited passage naming the same section of the same Act — because section numbers are not shared across Acts (s. 482 CrPC is the High Court's inherent power; s. 482 BNSS is anticipatory bail). Quotes cut with an ellipsis are checked piece by piece, in order. The unsupported-claim check covers "Short answer" and "How it may apply" as well as "What the sources say", but not statements that the sources are silent. Validator log records carry counts, not model text, since model text can echo personal data from the query. The prompt adds "How it may apply" (grounding note) and "which missing facts matter" | replay of the 52 saved model answers of `20260924T002232Z`: jurisdiction-line removals 45 → 0; first attempts that would regenerate 2 → 1, none newly triggered |
| D40 | **High stakes and refusals.** The grounding note's refusal rule is now implemented (it did not exist): a first-person request to fabricate or backdate documents, forge, destroy or tamper with evidence, bribe, threaten, abscond or evade process is refused before retrieval, with a lawful alternative; questions about being evicted, threatened or arrested are not refused (12 unit cases). When the evidence gate fails on a high-stakes question, the abstention keeps the safety block, lists the facts an answer turns on (state, written agreement, rent period and notice dates for tenancy; dates, offence, FIR and custody for criminal questions), and shows up to two top-ranked statute passages verbatim, labelled as unconfirmed — still without calling the LLM (non-negotiable 2). Tenancy questions that name no state get a note that no state tenancy law is ingested | grounding note, "High-stakes handling and refusals" |
| D41 | **Index integrity.** A `vectors` table records the sha1 of the embed_text each FAISS vector came from, and `sync_index` re-embeds when it differs. `chunks.rowid` is the FAISS id and SQLite reuses the highest rowids after a delete, so re-indexing a changed document could keep an old vector for new text (`test_reused_rowid_with_new_text_is_re_embedded` failed on the old code). The existing index is bootstrapped from the audit in V33. Dropped duplicates (`(doc_id, sha1(text))`) are written to `rejects` with the locator that kept the text. PDF-derived chunks must carry `page_start`/`page_end`, and OCR chunks `ocr_confidence` (the chunk-schema note called them required but the code did not check) | V33; unit tests |
| D42 | **Sources: nothing new ingested.** The indexed BNSS, RERA and TPA copies are current and verified (V30–V32). The CrPC 1973 stays out (D20 reaffirmed): no reachable official copy is the text as in force on 30 June 2024 (V34). No Hindi text is ingested: the official Hindi TPA has a broken font mapping and the Hindi BNSS has no text layer, and Tesseract is not installed (V35). No state rent law is added: the eviction question names no state | V29–V35 |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V29 | Correction to V11: on 2026-09-24 some legacy `https://www.indiacode.nic.in/indiacode/bitstream/...` URLs still serve PDFs (legacy handles 2158 RERA, 2338 and 14648 TPA), while others return 504 on all three host forms (21613 CrPC, 20099 and 20335 BNSS, 2338/2 Hindi TPA). Legacy handle numbers do not resolve on indiacode.gov.in (`/server/api/pid/find` → 404 for all six) | curl and API calls, 2026-09-24 |
| V30 | TPA: `tpa.pdf` (legacy handle 14648) prints the pre-2003 s. 106 ("fifteen days' notice expiring with the end of a month of the tenancy", no sub-sections (2)–(4)): stale, rejected. Legacy 2338 `A1882-04.pdf` has MD5 `765e702a927c908ae119f5d9f88380fb`, the repository MD5 of the indexed copy, whose s. 106 carries "Subs. by Act 3 of 2003, s. 2, for s. 106 (w.e.f. 31-12-2002)" | chunk `1915e99dae6b:s-106:1ea13bcf` |
| V31 | RERA: legacy 2158 `A201616.pdf` (461,032 bytes) and the indexed copy have identical s. 18 text (1,909 characters); both print the amendment of s. 68 by the Jan Vishwas (Amendment of Provisions) Act, 2026 (8 of 2026), w.e.f. 07.05.2026 | line diff, 2026-09-24 |
| V32 | BNSS ss. 482 and 531 in the indexed India Code copy match the Gazette of India text CG-DL-E-25122023-250884 (MHA copy `https://www.mha.gov.in/sites/default/files/2024-04/250884_2_english_01042024.pdf`, 249 pp.) apart from marginal headings and page running heads; India Code prints no BNSS amendment beyond the commencement footnote | normalised diff, 2026-09-24 |
| V33 | All 27,560 FAISS vectors match fresh bge-m3 embeddings of their chunks' embed_text (minimum cosine 0.99976, none below 0.99; 404 s) | re-embedding audit, 2026-09-24 |
| V34 | CrPC candidates: India Code Chandigarh item `123456789/620053` and the Supreme Court Legal Services Committee copy (sclsc.gov.in) cite amending Acts only up to 2014 and print s. 438(1) as "When any person …" with no sub-section (4); Punjab item `123456789/586487` is "with amendments of Punjab State" (amendments to 1993); the central item `620185` is the 1974 as-enacted scan (V23). The Supreme Court quotes the later text: Nathu Singh v. State of U.P. (2021) para 17 ("Where any person …", the substituted s. 438(1)) and Dhanraj Aswani v. Amar S. Mulchandani, [2024] 9 S.C.R. 257, para 7 (s. 438(4)). The Legislative Department page `legislative.gov.in/actsofparliamentfromtheyear/code-criminal-procedure-act-1973` rendered no content, and `webapi.sci.gov.in` refused connections | 2026-09-24 |
| V35 | Hindi: India Code `H1882-04.pdf` (TPA) has a Devanagari text layer with a broken font mapping (अधिनियम → "अत्तित्तियम", जुलाई → "जुिाई"; ल and न both extract as ि), identically in pdfplumber and pypdfium2; `202346.pdf` (Hindi BNSS, 76 MB, 319 pp.) extracts only digits and punctuation. `tesseract` is not installed | extraction tests, 2026-09-24 |
| V36 | Correction to V3's evidence: the judgment itself (dataset PDF `2020_3_1_328_EN.pdf`, first page) reads "[2020] 3 S.C.R. 1 … INDORE DEVELOPMENT AUTHORITY v. MANOHARLAL & ORS. ETC. (S.L.P. (C) Nos. 9036-9038 of 2016) MARCH 06, 2020 [ARUN MISHRA, INDIRA BANERJEE, VINEET SARAN, M. R. SHAH AND S. RAVINDRA BHAT, JJ.]"; the SLP matches diary 8700/2016 in the SCI URL (unreachable today). "(2020) 8 SCC 129" is quoted in 84 indexed chunks of later Supreme Court judgments. The 2023 judgment V3 cites only quotes the decision | raw PDF and SQLite, 2026-09-24 |
| V37 | Windows Smart App Control is on (`VerifiedAndReputablePolicyState` = 1) and since 09:18 on 2026-09-24 blocks the uv Python from loading `torch\lib\caffe2_nvrtc.dll` (CodeIntegrity events 3033, 3077, 3118; `WinError 4551`). The same code loaded torch earlier that day. faiss, pypdfium2, pdfplumber, tokenizers and Ollama still load. Ollama updated itself from 0.34.2 to 0.34.3 in the same period; `gemma4:latest` is still `c6eb396dbd59` | event log and registry read, 2026-09-24 |

## 2026-09-24 — AWS setup files for the user to run (Phase 3 preparation; nothing deployed by Claude)

### Decisions
| # | Decision | Reason |
|---|---|---|
| D43 | **AWS setup as Windows CMD, run by the user.** `infra\aws_setup.cmd` (menu) and `infra\aws_teardown.cmd` replace the planned `deploy.sh`/`teardown.sh` on this Windows machine. Two stacks: `praetor-app` in ap-south-1 (S3 bucket: private, SSE-S3, BucketOwnerEnforced, TLS-only, versioned, noncurrent versions expire after 7 days, `DeletionPolicy: Retain`; managed policy `praetor-app` scoped to that bucket and to the ARNs resolved from the chosen model or inference profile) and `praetor-budget` in us-east-1 (AWS Budgets' home region; `IncludeCredit: false` so alerts track usage before credits). Resource-changing steps require typing YES (DELETE for teardown). The setup never invokes Bedrock: the "tiny test call" of the pre-flight becomes the app's first call through the cost meter after the user's go. It never stores credentials (profile via `aws configure sso` or `aws configure`), never calls Cost Explorer, and writes `.env` lines to `data\scratch\aws\env_lines.txt` instead of editing `.env` | user asked on 2026-09-24 for a CMD file to finish the AWS setup in parallel; aws-cost-plan note; non-negotiables 6–8 |


### Verifications
| # | Fact | Evidence |
|---|---|---|
| V38 | AWS CLI 2.34.4 is installed at `C:\Program Files\Amazon\AWSCLIV2` (a pip-installed `aws` also sits on PATH, so the scripts put v2 first); only the `default` profile exists. Option names used by the scripts exist in this CLI: `bedrock list-foundation-models --by-inference-type ON_DEMAND --by-output-modality TEXT`, `bedrock list-inference-profiles --type-equals SYSTEM_DEFINED`, `bedrock get-inference-profile --inference-profile-identifier`, `bedrock get-foundation-model --model-identifier`, `budgets describe-budgets --account-id`, `cloudformation deploy --capabilities --parameter-overrides --tags --no-fail-on-empty-changeset`, `iam list-entities-for-policy / attach-user-policy / detach-user-policy`. Budget `CostTypes.IncludeCredit` defaults to true | `aws <command> help`, 2026-09-24; no AWS API was called |

## 2026-09-24 — Verification of the audit repairs (torch loads again)

### Decisions
| # | Decision | Reason |
|---|---|---|
| D44 | **The transition pin pins only the successor's repeal section.** The successor's "closest provisions" (top 3 by similarity to the rewritten query) become their own fusion list and must pass the reranker. Pinning a similarity guess bypassed relevance: for the diagnostic "Can the High Court quash an FIR under section 482 CrPC?" it put BNSS s. 442 (High Court's powers of revision) at S1, while the inherent power is BNSS s. 528. For "s. 438 CrPC" the D36 rewrite now ranks BNSS s. 482 first on its own (reranker 0.92). Integration tests `test_guessed_successor_provision_is_not_pinned` and `test_reported_failures_reach_the_context` | `evaluation/reports/diagnostics/20260924_stage_trace_after.json`, label `diag:crpc-482-quash` |
| D45 | **Repeal first.** When a question names a repealed Act and the successor's pinned repeal section is in the context but the answer does not cite it, the single stricter retry adds an instruction to state the repeal first, citing that section, and to answer from the successor's provisions in the sources. A grounded first answer is kept if the retry is worse. No section correspondence is supplied by the system | first post-audit eval: `tr-438-anticipatory` cited neither BNSS s. 531 nor s. 482 although both were in context (S1, S2) and said the sources gave no current status |
| D46 | **Validator: sub-sections and unnamed mentions.** "17(1)" is section 17 (the new act-aware check had read it as sections 17 and 1 and removed two correct sentences). A passage's section mention without an Act ("Section 3 of the Act") counts for its own Act in statute text, and in other text only when the passage names exactly one Act — so "Section 3 of the Limitation Act" citing a judgment that says "Section 3 of the Act" and names only the Limitation Act is accepted, while a passage naming the CrPC and the IPC cannot lend its bare "Section 438" to the IPC | replay of the 54 saved answers of eval `20260924T094039Z` with their saved context ids: authority removals 9 → 5; no regeneration decision changes. The 5 left: 4 would also be removed by the pre-audit rule; 1 ("Section 49 of the 1908 Act", ambiguous between the Registration Act and the CPC) is removed by the stricter rule |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V39 | Stage trace after D36/D44 (same 13 queries as D35): BNSS s. 482 reaches the context for "How do I apply for anticipatory bail?" (reranker 0.025 → 0.99, final rank 12 → 2, S1), the Hindi question (not retrieved → final 4, S1) and "s. 438 CrPC" (0.002 → 0.92, final 15 → 1, S1); RERA s. 18 for the refund question (0.37 → 0.96, final 12 → 2, S2); the eviction question passes the gate (top rerank 0.025 → 0.82) with TPA s. 106 at S3; the lease-notice question is unchanged (S1) | `evaluation/reports/diagnostics/20260924_stage_trace_after.json` |
| V40 | Repeated runs (5 per question; tpa-106-lease-notice, rera-018-refund, proc-bnss-482-anticipatory, hs-eviction-tomorrow): the retrieval context is identical in all runs under both decodings, so variation starts in generation. Temperature 0 + seed 42: 2 distinct raw outputs per question (run 1 differs, runs 2–5 identical), so not bit-for-bit deterministic. Temperature 0.1, no seed (Phase 2): 5 distinct outputs per question. Under both, every run cites the required provision (TPA s. 106, RERA s. 18, BNSS s. 482) and contains the must-mention phrase; the other cited provisions vary more without the seed (eviction: 2 vs 4 distinct sets) | `evaluation/reports/repeat_20260924T092144Z.json` (new), `repeat_20260924T092619Z.json` (Phase 2 decoding) |
| V41 | First full eval after the audit (prompt `answer-v3`, D36–D44; before D45/D46): retrieval full R@5 0.91 → 0.96, R@10 0.93 → 1.00, MRR 0.84 → 0.85, in-context 1.00; hybrid + rerank R@5 0.89 → 0.94; keyword 0.44 → 0.46; dense 0.83 (unchanged, as designed). Abstention 5/5 out-of-corpus, false abstentions 1 → 0. Answers: 54 answered, 0 invalid citations after validation (per row), 100% cited, extractive fallbacks 1 → 0, must-mention 95% → 98% (n=44), expected source cited 92% → 98% (n=54), jurisdiction-line removals 45 → 0, confidence high/medium/low 29/6/24 (was 3/0/56), authority removals 1 → 9 (see D46), p50 11.5 → 13.5 s, output tokens 393 → 539. Exploratory: every question was seen during development (D38) | `evaluation/reports/20260924T094039Z.md` |
| V42 | **Final eval after D36–D46** (`evaluation/reports/20260924T174231Z`, prompt `answer-v3`, temperature 0, seed 42, `gemma4:latest` c6eb396dbd59, same index): retrieval full R@5 0.96, R@10 1.00, MRR 0.85, in-context 1.00 (pre-audit 0.91 / 0.93 / 0.84); abstention 5/5, 0 false (pre-audit 1 false). Answers: 54 answered, **0 invalid citations after validation, checked per row**, 100% cited, expected source cited 98% (n=54; pre-audit 92%), must-mention 95% (n=44), regime note 100% (n=5), confidence high/medium/low 26/6/27 (pre-audit 3/0/56), p50 13.4 s. Two answers fell back to extractive because Ollama timed out (one question took ~60 min wall time, far beyond the 180 s timeout, consistent with the machine pausing); re-run afterwards, both answered normally with the expected source and must-mention phrase. Authority removals 6: 2 correct catches (proc-bnss-482: the model wrote "under the BNSS ... section 438" and attributed the CrPC s. 438 factors to the BNSS), 2 that the pre-audit rule also makes (hs-limitation), 2 costs of the strict rule (tr-438: a judgment saying only "Section 438(1) of the Code"; cl-unregistered: "the 1908 Act"). All 7 questions naming a repealed Act cite the successor's repeal section; tr-438 now leads with the repeal (D45) but still does not cite BNSS s. 482 although it is in context (model limitation). Integration tests 16/16 (11 + 5 new). Exploratory: every question was seen during development (D38) | `evaluation/reports/20260924T174231Z.md`; targeted re-run and transition check 2026-09-24 |

## 2026-09-25 — AWS dropped; Phases 3 and 4 local; Vercel for the frontend

### Decisions
| # | Decision | Reason |
|---|---|---|
| D47 | **No AWS; everything runs locally; Vercel hosts only the frontend.** The user's direction of 2026-09-25: drop AWS, deploy with Vercel, finish Phases 3 and 4 on the local machine, keep the GitHub repository under 10 MB; the frontend is built separately (GPT-6 Astra). Consequences: `infra/` (AWS scripts and templates, never deployed — the user confirmed nothing was created) is deleted; the Bedrock provider, cost meter plan, S3 sync and AWS settings are removed (`LLM_ANSWER` is `ollama` or `extractive`); non-negotiable 8 becomes "no paid services". The backend cannot run on Vercel (V43), so it stays on the laptop as a FastAPI server (`praetor serve`) and a Vercel-hosted frontend reaches it through a tunnel, calling it from server-side code that holds `API_KEY`. The Supreme Court judgments open dataset stays a data source: it is hosted on AWS Open Data but read anonymously (no account, no cost) | user message 2026-09-25; V43 |
| D48 | **Phase 3 redefined: the local API.** `POST /v1/ask`, `GET /v1/sources/{chunk_id}`, `GET /v1/healthz`, `GET /v1/stats`; contract exported to `docs/api/openapi.json` (`praetor openapi`) so the frontend can be built in parallel. One question at a time on the GPU; `X-Request-ID` equals the answer's trace id; errors as `{"error": {code, message, request_id}}`; with `API_KEY` set every endpoint but `/v1/healthz` needs `X-API-Key`, without it only direct localhost calls are served (tunnel traffic carries forwarding headers and is refused); `praetor serve` refuses a non-localhost bind without a key; CORS allowlist from `CORS_ORIGINS`. The answer cache (SQLite `cache` table, unused since Phase 1) is implemented: key = hash of the normalised question plus prompt version, model and digest, decoding, corpus hash, registries and gate; transient failures are not cached; the question itself is never stored. Phase 4: README from zero to demo, `scripts/demo.py`, repository-size guard, final report | build-plan note (updated) |
| D49 | **Repository size.** Full eval dumps (`evaluation/reports/2*.json`, ~400 KB each) are no longer committed — their Markdown summaries are; a unit test fails if tracked files exceed 8 MB. Model weights, corpora, SQLite and FAISS were already gitignored | V44 |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V43 | Vercel Functions limits: memory 2 GB (Hobby) / 4 GB (Pro); uncompressed bundle 250 MB, 500 MB for Python, up to 5 GB with large functions (beta); max duration 300 s on Hobby (800 s Pro); request/response body 4.5 MB; no GPU offered. The backend needs bge-m3 (4.3 GB on disk), bge-reranker-v2-m3 (2.2 GB), torch, and a local LLM (`gemma4:latest`, 9.6 GB) — it does not fit | https://vercel.com/docs/functions/limitations (page updated 2026-08-24), read 2026-09-25; `du` of the Hugging Face cache |
| V44 | Repository size on 2026-09-25: 3.51 MB of tracked files (163 files), 1.21 MiB of git objects; largest items are full eval JSON dumps (~400 KB each) and `uv.lock` (275 KB); no git remote yet | `git count-objects -vH`, `git ls-files` |
| V45 | Smart App Control blocks again on 2026-09-25: `praetor.exe` (uv launcher) and scikit-learn's `_datasets_pair` DLL, which `sentence_transformers` imports, so the embedder and reranker cannot load; `torch` itself loads. The API unit tests (stubbed engine) pass; the real-engine API test and the demo wait for the block to clear. `praetor.cmd` runs the CLI through Python for the launcher part | import tests, 2026-09-25 |

## 2026-09-25 — Document mode: the user's own PDFs (hackathon brief "AI for Legal Assistance & Access")

### Decisions
| # | Decision | Why / source |
|---|---|---|
| D50 | **Uploaded documents, analysed but never indexed.** The user's direction of 2026-09-25: cover the brief's document use cases (answer questions on a provided document, simplify, highlight clauses/obligations/risks/inconsistencies, compare agreements, checklists, questions for a lawyer). New `app/documents/` (parse → passages with the document's own clause/section/para numbers and pages; in-memory store; analysis) and endpoints `POST /v1/documents`, `GET`/`DELETE /v1/documents/{id}`, `POST /v1/documents/analyze` with tasks `ask`, `summary`, `risks`, `checklist`, `lawyer_questions`, `compare`; prompt `document-v1`. Same grounding contract as `/v1/ask`: numbered passages only, the deterministic validator (it gained a `claim_sections` argument for the task headings), one stricter retry, verbatim fallback; `[D#]` cites the user's passages and `[S#]` law, cards from stored metadata. Uploads are personal data and lack verified provenance, so they are never added to the corpus, cached, logged or written to disk: memory only, 60-minute expiry, explicit delete. Law cross-check: when the engine is loaded, up to 3 corpus passages that pass the evidence gate join the context (the model may compare a clause with an Act only when a LAW passage covers it, and never calls a clause void or illegal). Documents run without the embedder, reranker or index, so this works while V45 blocks `sentence_transformers`. Long documents: 16k-token window (`DOC_NUM_CTX`) with ~14k estimated passage tokens; the opening plus BM25-selected passages when a document does not fit, with a warning and `documents[].complete=false` (no map-reduce: time). Scanned PDFs are refused with a clear message (no OCR, D9). Runs of 6+ markers on one sentence are cut to the first 3 (the model cited D1–D44 in one sentence once). `python-multipart` added (FastAPI's multipart parser). Also fixed: a failed warm-up left the engine assigned, so `/v1/ask` returned 500 instead of 503; `/v1/healthz` adds `checks.documents` | user message 2026-09-25 with the brief; grounding note; non-negotiables 1, 5, 6 |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V46 | Start of session: `import sentence_transformers` still fails (Smart App Control, V45), so the integration tests and `scripts/demo.py` were not run. Document mode verified end to end on the real server (`.\praetor serve`, engine not loaded, `gemma4:latest` c6eb396dbd59, temperature 0, seed 42) with two official model agreements: Punjab RERA "Agreement for Sale" (Punjab Govt. Gazette (Extra), 8 June 2017, Annexure A, https://rera.punjab.gov.in/pdf/annexures/rera-punjab-agreement-for-sale.pdf, 24 pp. → 49 passages) and MahaRERA "Model Form of Agreement" (https://maharera.maharashtra.gov.in/sites/default/files/inline-files/afs.pdf, 24 pp. → 53 passages), downloaded 2026-09-25 to `data/scratch/docs/` (gitignored). `scripts/demo_documents.py`: ask, summary, risks, checklist, lawyer_questions, compare all 200 and OK (every marker has a card and every card is cited; every document card's quote is verbatim in its passage); 12–21 s each; provider ollama, no retries or fallbacks; confidence medium (documents longer than one pass: 44 of 49 passages read). Read by hand: the delayed-possession answer cites clause 7 terms (refund with interest within ninety days; the allottee may stop payments); compare cites Punjab cl. 7.5 (10% forfeiture, balance within ninety days) against the MahaRERA refund within thirty days of termination. gemma4 at a 16k window: 3.2 GB, 100% GPU, 4.5 GB used on the card with the encoders not loaded. The token estimate is ~30% above the real count (≈ 11.9k real input tokens at the 14k budget). Law cross-check not verified live (needs the engine; unit-tested with a stubbed law lookup returning the real TPA s. 106 chunk). Unit tests 114 | `docs/api/examples/document_*.json`; `ollama ps`; `nvidia-smi` |

## 2026-09-25 — Deployment: GitHub, API key, Cloudflare quick tunnel; end-to-end verification

### Decisions
| # | Decision | Why / source |
|---|---|---|
| D51 | **Deployment path.** Code on GitHub at https://github.com/Harshiee09/PRAETOR-AI (public; the user's repository, empty until this push; history scanned first: only `.env.example` ever committed, no key patterns). `API_KEY` generated (43 characters, `secrets.token_urlsafe(32)`) into the gitignored `.env` and never printed. Tunnel: Cloudflare quick tunnel (no account; the user's choice) with `cloudflared.exe` 2026.9.3 in `C:\dev\tools`, outside the repository; `scripts\serve_public.cmd` starts the API and the tunnel. The URL changes on every start, so the Vercel variable `PRAETOR_API_URL` must be updated each time; a named tunnel or ngrok static domain would fix that and needs an account. Frontend: `frontend/` of the same repository with Vercel Root Directory `frontend` (recommended, not yet built). The API integration test client now sends `X-API-Key` when one is configured | user message 2026-09-25; deployment note |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V47 | **Smart App Control no longer blocks `sentence_transformers`** (6.1.0 imports; the engine loads on CUDA). Integration tests **19/19** (16 + the 3 API tests; the first run had 2 failures, both 401 because `.env` now has a key and the test client sent none — fixed in the fixture, then 3/3). `scripts/demo.py --fresh --save-examples docs/api/examples`: **7/7 OK** (statute 13.1 s, procedure 12.5 s, case law 13.2 s, transition 22.4 s, Hindi 10.4 s, out of corpus 0.8 s, high stakes 10.3 s); saved answers read: every [S#] has a card, the Schengen question abstains with no citations, the eviction answer starts with the safety block and carries the state-law note. Cosmetic: the eviction short answer contains an empty "[]" written by the model. Through the public quick tunnel: healthz 200 without a key; upload without a key and with a wrong key 401; with the key upload 201, document ask 200 in 19 s citing [D21] and a law passage [S1] (first live law cross-check; a live `risks` run passed the law gate at 0.71 but the model did not use the law passages), request id echoed; library ask 200 in 15 s citing TPA s. 106. `cloudflared-windows-amd64.exe` SHA-256 matches the release notes. Cloudflare limits: 524 after the 125 s Proxy Read Timeout (Enterprise can raise it); quick tunnels: 200 in-flight requests, no SSE, no SLA, "for testing and development only" | https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-5xx-errors/error-524/ and https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/do-more-with-tunnels/trycloudflare/, read 2026-09-25; `docs/api/examples/ask_*.json` |

## 2026-09-25 — Frontend in the repository

### Decisions
| # | Decision | Why / source |
|---|---|---|
| D52 | **The GPT-built frontend moves into `frontend/`** (from the user's OneDrive folder; `node_modules`, `.next` and build info excluded; own `.gitignore`, `.env.local` holds the key locally). Vercel builds it with Root Directory `frontend`. Repairs and completion: the document page had no CSS at all (about 40 classes undefined) — new `src/app/document.css` in the existing monochrome tokens; the user's reference components are now all on screen: Velaris (Ask hero), **Gateway Flow** (document hero, native canvas, no CDN iframe), **Flow Button** (Ask and document submit), Header1 (navigation), **Comparison-2** ("Two ways in" on Ask, and Document A vs B cards on compare results filled from the real citations); comparison-card buttons given explicit contrast; three failing workflow tests fixed (a space inside screen-reader-only spans made accessible names "View textof Document A"; the restore test waited for an upload control a restored document does not show) | user message 2026-09-25 with the reference components |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V48 | Frontend: `tsc` clean, **119/119** tests, `next build` OK. Live against the real API through the frontend proxy (`next dev` on :3001, `praetor serve`): library question answered with an [S1] card (TPA s. 106); document upload (24 pages, 49 passages, 60-minute expiry); `risks` result with [D#] chips, "Your document · A" cards and pages-read line (30 s); `compare` of the Punjab and MahaRERA model agreements with the A/B comparison cards listing the cited clauses (19 s); light and dark themes and a 375 px phone layout checked by screenshot | browser pane, 2026-09-25 |

## 2026-09-26 — Alternative deployment: the whole app behind one link

### Decisions
| # | Decision | Why / source |
|---|---|---|
| D53 | **One public link for the whole app, served from the laptop** (`scripts\serve_app_public.cmd`): the API on 127.0.0.1:8000 (never exposed), the production website (`next build` + `next start`) on 127.0.0.1:3100, and one Cloudflare quick tunnel to the website. The website's server-side routes call the API locally with the key from `frontend\.env.local`, so no key or API URL lives in any cloud service and nothing needs updating when the link changes; the link is printed and saved to `data\scratch\app-tunnel.log`. Vercel (D51) stays possible but is not needed | user message 2026-09-26 ("take an alternate route"): the quick-tunnel URL kept changing after windows were closed or Ctrl+C was pressed, and Vercel's environment variable had to follow it |

### Verifications
| # | Fact | Evidence |
|---|---|---|
| V49 | Through the public link (`https://disposition-charm-tray-abilities.trycloudflare.com`, 2026-09-26): `/`, `/document`, `/about` 200; `/api/health` all checks ok; library ask 200 in 19 s citing Registration Act ss. 23, 25, 26; PDF upload 201 (24 pages, 49 passages); document ask 200 in 24 s citing [D25]; delete 204 | scripted check, 2026-09-26 |
